# AutoQQ 通用 EventServer 操作指南

> 文档状态：测试环境执行中。2026-09-14 已经用户授权完成本地依赖、隔离
> `autoqq_test` 建库/迁移回退、MySQL 并发测试与受控 Warframe 基线采集；2026-09-15
> 已完成隔离远端数据库、真实 Hermes/QQBot Plugin 主动私聊和 `claim/send/ack` 联调。
> 正式库、公网 Webhook、备份恢复演练和生产部署仍需单独授权。
>
> 本项目与 `plugin` 为两个独立代码项目、独立目录。EventServer 是唯一允许访问业务数据库的组件。根目录 `qqBot.md` 中的 Warframe 需求作为首批事件案例，不再限定 EventServer 的领域边界。

## 1. 项目定位

EventServer 是通用事件采集、判定、发布和投递服务。核心层不得依赖 Warframe 概念；Warframe 只是第一组可插拔事件 provider。

EventServer 负责：

- 用户、角色、账号状态、`chat`/`command` 双维度权限、订阅关系和管理员审计。
- 事件类型注册、版本管理和启停控制。
- 通过轮询、定时任务、受信 Webhook 或内部发布接口接入事件源。
- 将来源各异的数据转换为统一事件观察模型。
- 比较状态、应用确定性触发规则并产生标准事件。
- 为有效订阅者生成幂等的待投递记录。
- 向 Hermes Plugin 提供带租约的消息领取接口。
- 接收 Plugin 的发送成功/失败回写并安排有限重试。
- 提供内部 HTTP API、健康检查、指标和结构化日志。

EventServer 不负责：

- 直接调用 Hermes 或腾讯 QQ API。
- 保存 QQ AppID、QQ Secret 或 LLM Key。
- 调用 LLM 判断事件、权限、订阅对象或通知内容。
- 允许普通用户提交任意 URL、脚本、SQL 或表达式作为事件源。
- 将某一事件源的字段泄漏到核心调度、订阅或投递模块。

## 2. 架构分层

```text
外部数据源 / 内部系统
  ├── Polling Source Adapter
  ├── Scheduled Source Adapter
  ├── Signed Webhook Adapter
  └── Trusted Internal Publisher
              │
              ▼
       Observation Normalizer
              │ 统一 Observation
              ▼
        Trigger / Change Detector
              │ 统一 DomainEvent
              ▼
       Event Store + Subscription
              │
              ▼
          Delivery Outbox
              │ claim / lease / ack / fail
              ▼
       Hermes AutoQQ Plugin
              │
              ▼
         QQ 用户私聊
```

核心模块只处理统一接口和统一数据结构：

- `source`：取得原始数据，不负责通知。
- `normalizer`：校验并转换数据，不访问用户订阅。
- `trigger`：比较观察结果并确定是否发布事件。
- `renderer`：把已发布事件转换为渠道无关的通知内容。
- `subscription`：根据稳定 `event_key` 查找有效订阅者。
- `outbox`：创建、领取、回写和重试投递任务。
- `channel consumer`：由外部 Plugin 实现；EventServer 不包含 QQ 发送代码。

任何新事件接入都应新增 adapter/provider 包，而不是修改核心投递流程。

## 3. 独立项目与计划目录

`eventserver` 应能独立构建、测试、迁移和部署。它与 Plugin 只共享版本化 HTTP 契约，不共享源代码目录、虚拟环境、配置文件或数据库连接。

建议后续目录结构：

```text
eventserver/
├── README.md
├── Dockerfile
├── pyproject.toml
├── .env.example
├── migrations/
├── src/
│   └── eventserver/
│       ├── api/
│       │   ├── public_events.py
│       │   ├── subscriptions.py
│       │   ├── deliveries.py
│       │   └── webhooks.py
│       ├── auth/
│       ├── db/
│       ├── core/
│       │   ├── models.py
│       │   ├── provider.py
│       │   ├── trigger.py
│       │   ├── renderer.py
│       │   └── registry.py
│       ├── scheduler/
│       ├── deliveries/
│       │   ├── outbox.py
│       │   └── leases.py
│       └── providers/
│           ├── warframe/
│           │   ├── cetus_night.py
│           │   ├── konzu_rotation.py
│           │   └── ghoul_event.py
│           └── example/
├── tests/
│   ├── contract/
│   ├── core/
│   └── providers/
└── docs/
    ├── provider-development.md
    └── event-catalog.md
```

若使用 Python，建议采用 FastAPI、SQLAlchemy 2.x、Alembic、MySQL 驱动和 httpx。provider 发现机制可采用显式注册表或 Python entry points，但生产启用列表必须可审计，不能扫描并执行任意上传代码。

## 4. MySQL 目标配置

已确定的目标连接参数：

| 项目 | 值 |
| --- | --- |
| 数据库类型 | MySQL |
| 主机 | `192.168.1.4` |
| 端口 | `3309` |
| 用户 | `root` |
| 数据库名 | `autoqq`（当前建议值，可配置） |
| 字符集 | `utf8mb4` |
| 时区 | 数据库存 UTC，展示默认 `Asia/Shanghai` |

数据库口令属于 Secret，不写入仓库或操作指南。用户提供的口令只应在实际执行时写入本机未跟踪的 `.env`、容器 Secret 或密钥管理系统。若口令含 `#`、`@`、`:` 等字符，优先使用独立的 `MYSQL_PASSWORD`；拼接 URL 时必须百分号编码。

后续 `.env.example` 建议内容：

```dotenv
MYSQL_HOST=192.168.1.4
MYSQL_PORT=3309
MYSQL_DATABASE=autoqq
MYSQL_USER=root
MYSQL_PASSWORD=replace-locally-do-not-commit
MYSQL_CHARSET=utf8mb4

INTERNAL_API_TOKEN=replace-with-a-long-random-secret
INITIAL_ADMIN_OPENIDS=
DEFAULT_TIMEZONE=Asia/Shanghai
ENABLED_PROVIDERS=warframe.cetus_night,warframe.konzu_rotation,warframe.ghoul_event
DELIVERY_MAX_ATTEMPTS=5
DELIVERY_DEFAULT_LEASE_SECONDS=60
PROVIDER_DEFAULT_TIMEOUT_SECONDS=10
```

生产环境不建议长期使用 MySQL `root`。首次验证通过后，应在获得运维授权的前提下创建仅对 `autoqq` 数据库拥有必要权限的专用账号并限制来源网段。

## 5. 通用事件标识与版本规则

事件键采用稳定的命名空间格式：

```text
<domain>.<resource>.<event>
```

首批示例：

| `event_key` | 显示名称 | provider |
| --- | --- | --- |
| `warframe.cetus.night` | 希图斯进入夜晚 | `providers.warframe.cetus_night` |
| `warframe.konzu.rotation` | Konzu 赏金轮换 | `providers.warframe.konzu_rotation` |
| `warframe.ghoul.started` | 尸鬼活动出现 | `providers.warframe.ghoul_event` |

规则：

- `event_key` 一经发布不得改变含义；语义变化应创建新键或提升 schema 版本。
- 事件 payload 使用独立的正整数 `schema_version`。
- 显示名称、描述和模板可以修改，但不得用显示名称作为订阅主键。
- provider 实现版本与事件 schema 版本分开管理。
- 旧键需要下线时先标记 deprecated，提供迁移映射和观察期，再停止新订阅。
- 为兼容根文档中的旧键，可在迁移期配置别名映射；数据库最终只保存规范键。

## 6. 统一数据契约

### 6.1 Observation

来源数据经 normalizer 转换为观察结果：

```json
{
  "provider_key": "warframe.cetus_night",
  "schema_version": 1,
  "identity": "stable-source-identity",
  "observed_at": "ISO-8601 UTC",
  "effective_at": "ISO-8601 UTC",
  "expires_at": "ISO-8601 UTC or null",
  "state": {},
  "source_metadata": {
    "source": "warframe-worldstate"
  }
}
```

`identity` 必须稳定且可复现。`state` 必须通过该 provider 的版本化 schema 校验。`source_metadata` 不得包含访问令牌或大体积原始响应。

### 6.2 DomainEvent

trigger 根据当前与历史 Observation 产生标准事件：

```json
{
  "event_id": "deterministic-or-server-generated-id",
  "event_key": "warframe.cetus.night",
  "schema_version": 1,
  "dedupe_key": "stable-deduplication-key",
  "occurred_at": "ISO-8601 UTC",
  "subject": {
    "type": "location",
    "id": "cetus"
  },
  "data": {},
  "metadata": {
    "provider_key": "warframe.cetus_night"
  }
}
```

核心层只依赖 `event_key`、`schema_version`、`dedupe_key`、时间和通用 envelope。领域字段全部放在 `subject` 与 `data` 中。

### 6.3 DeliveryMessage

renderer 产生渠道无关的消息：

```json
{
  "title": "事件提醒",
  "text": "希图斯已进入夜晚",
  "data": {},
  "template_key": "warframe.cetus.night.default",
  "template_version": 1
}
```

第一版以纯文本为主。Plugin 负责验证 QQ 平台限制和安全分段；EventServer 不在领域 provider 中硬编码 QQ API 参数。

## 7. Provider 扩展接口

每个 provider 包实现一组公共能力，并按接入模式声明 capability。实际函数签名在编码阶段锁定：

```text
metadata() -> ProviderMetadata
validate_config(config) -> ValidatedConfig
normalize(raw) -> Observation
evaluate(previous, current) -> list[DomainEvent]
render(event, locale) -> DeliveryMessage
health() -> ProviderHealth
```

按接入模式选择实现，不能强迫所有 provider 依赖轮询：

```text
# polling capability
collect(context, checkpoint) -> RawObservation | NoChange

# scheduled capability
on_schedule(context, scheduled_at) -> Observation | DomainEvent

# webhook capability
verify_webhook(headers, body) -> VerifiedWebhook
ingest_webhook(verified) -> Observation | DomainEvent

# trusted publisher capability
validate_published_event(identity, payload, dedupe_key) -> DomainEvent
```

约束：

- provider 不得读写用户、订阅或 delivery 表。
- provider 不得直接发送消息或调用 LLM。
- provider 通过受控客户端访问外部数据源，必须设置超时、响应大小上限、User-Agent 和有限重试。
- provider 返回的数据必须经过 schema 校验后才能进入核心事件存储。
- provider 异常只能影响自身调度，不得阻塞其他 provider 或内部 API。
- 每个 provider 有独立的并发上限、速率限制、熔断状态和最近成功时间。
- provider 配置只允许来自管理员配置或受控部署，不接受普通用户任意 URL。

## 8. 事件接入模式

### 8.1 轮询型

适用于 Warframe WorldState、状态 API 等。scheduler 按 provider 配置调度，使用分布式租约避免多个 EventServer 实例重复轮询。失败时保留旧状态，不得把请求失败误判为状态变化。

### 8.2 定时型

适用于固定日程、周期提醒和内部计划任务。时间计算统一使用 UTC，展示时再按订阅者或系统时区转换。必须为同一时间窗口生成稳定 `dedupe_key`。

### 8.3 Webhook 型

适用于外部系统主动推送。每个来源使用独立路径、签名密钥、时间戳校验和重放保护。请求先持久化最小接收记录，再异步规范化；不得在 Webhook 请求线程中执行通知发送。

### 8.4 可信内部发布

后续可提供受服务身份保护的发布 API。发布者只能发布其被授权的事件命名空间，payload 必须符合已注册 schema，并提供幂等键。不得提供匿名或普通用户可调用的任意事件发布接口。

## 9. MySQL 数据模型

实施时使用 MySQL 8 兼容迁移，建议至少包含：

| 表 | 主键/唯一约束 | 用途 |
| --- | --- | --- |
| `users` | PK `(platform, openid)` | 用户、角色和 `active/blocked` 账号状态 |
| `user_permissions` | PK `(platform, openid, permission_key)` | 独立保存 `chat`、`command` 权限 |
| `event_types` | PK `event_key` | 通用事件目录、schema 与启停状态 |
| `event_aliases` | PK `alias_key` | 旧键到规范键的迁移映射 |
| `provider_instances` | PK `provider_instance_id` | provider 类型、受控配置和调度策略 |
| `provider_checkpoints` | PK `provider_instance_id` | 游标、最近状态哈希和调度租约 |
| `observations` | PK `observation_id`；UNIQUE `(provider_instance_id, identity)` | 可选的标准化观察历史 |
| `event_occurrences` | PK `event_id`；UNIQUE `(event_key, dedupe_key)` | 已发布的标准事件 |
| `subscriptions` | PK `(platform, openid, event_key)` | 用户订阅 |
| `deliveries` | PK `delivery_id`；UNIQUE `(event_id, platform, openid)` | 每用户待投递、租约和重试 |
| `pairing_codes` | PK `code_hash` | 一次性授权码 |
| `audit_logs` | PK `audit_id` | 管理与配置变更审计 |
| `webhook_receipts` | PK `receipt_id`；UNIQUE `(source_key, external_id)` | Webhook 重放保护与处理状态 |

迁移要求：

- 存储引擎统一使用 `InnoDB`，字符集 `utf8mb4`。
- 参与主键和索引的标识使用有明确上限的 `VARCHAR`。
- `user_permissions.permission_key` 第一版只允许 `chat`、`command`，使用唯一约束并由应用层严格校验。
- 账号状态与具体权限分离；`blocked` 优先于所有权限，删除权限不能代替封禁。
- JSON payload、schema 和配置使用 MySQL `JSON`，敏感配置只保存 Secret 引用，不保存明文。
- 时间字段使用 UTC 的 `DATETIME(6)` 或 `TIMESTAMP(6)`。
- 外键列的类型、长度、字符集和排序规则必须完全一致。
- `event_occurrences(event_key, dedupe_key)` 和 `deliveries(event_id, platform, openid)` 必须唯一。
- `deliveries` 至少包含 `status`、`attempts`、`next_attempt_at`、`lease_owner`、`lease_token_hash`、`lease_until`、`last_error_code` 和更新时间。
- 为可调度 provider、可领取 delivery、过期授权码和 Webhook 去重建立必要索引。
- 不使用 SQLite WAL 或 `busy_timeout`；配置连接池、事务隔离、锁等待超时和有限死锁重试。

迁移必须由 Alembic 或选定框架的正式迁移工具管理。应用启动时不得静默修改生产表结构。

## 10. 数据库准备流程（测试库已执行）

执行记录（2026-09-14）：步骤 1—7 和测试环境的步骤 9 已完成。原配置的
`192.168.1.4:3309` 未监听，只读确认同主机 `3306` 为 MySQL 8.4.11 后，仅修正
本地未跟踪 `.env`。`autoqq_test` 已完成 upgrade/downgrade/upgrade 并位于
Alembic head。步骤 8 的正式库迁移和步骤 10 的生产备份记录仍待单独授权。

1. 从 EventServer 运行主机验证 `192.168.1.4:3309` 网络可达性。
2. 使用本地 Secret 注入的凭据验证 MySQL 版本与 TLS 能力。
3. 确认 `autoqq` 数据库及当前账号权限现状。
4. 如获授权，创建数据库并设置 `utf8mb4` 字符集和合适排序规则。
5. 生成首个迁移，人工审阅表、外键、索引和删除行为。
6. 对空测试库执行升级、降级、再升级。
7. 运行核心、provider、API、数据库和并发幂等测试。
8. 经确认后才对目标数据库执行迁移。
9. 注册首批事件类型、Warframe provider 实例和初始管理员；初始管理员显式拥有 `chat=true`、`command=true`。
10. 记录迁移版本、执行人、时间与备份位置。

数据库创建和授权命令必须在确认现状后生成，不能直接套用未验证示例。

### 10.1 不知道首管理员 OpenID 时的一次性引导

全新 `autoqq` 数据库可以在 `INITIAL_ADMIN_OPENIDS=` 为空时执行基础 bootstrap；它只注册事件、
别名和 provider。EventServer 与 Plugin 启动后，预定管理员向机器人发送消息，Plugin 使用可信
QQ 消息上下文中的 OpenID 创建 pairing code。运维人员随后只在部署主机的交互终端执行：

```bash
python -m eventserver.bootstrap_admin
```

Docker Compose 部署应使用包装脚本：

```bash
./deploy/scripts/bootstrap-admin.sh
```

安全边界：

- pairing code 通过隐藏输入读取，不支持明文命令行参数。
- 命令只允许本地执行，不注册 HTTP 路由。
- 只有不存在 `active + role=admin + command=true` 用户时才能成功。
- MySQL 使用连接级命名锁串行化并发引导，配对记录再以 `FOR UPDATE` 锁定。
- 配对码必须属于 `qqbot`、尚未过期且未消费；显式封禁目标被拒绝。
- 用户状态、admin 角色、`chat`/`command`、配对码消费和脱敏审计在同一事务提交。
- 标准输出和错误输出都不得包含完整 OpenID 或 pairing code。
- 建立首管理员后，该命令拒绝再次引导；后续用户由管理员批准 pairing code。

## 11. 内部 HTTP API

第一版对 Plugin 提供：

```text
GET    /health
GET    /v1/events

GET    /v1/users/{openid}/permission
POST   /v1/users/{openid}/grant
POST   /v1/users/{openid}/revoke
POST   /v1/users/{openid}/role

GET    /v1/users/{openid}/subscriptions
POST   /v1/users/{openid}/subscriptions
DELETE /v1/users/{openid}/subscriptions/{event_key}

POST   /v1/pairing-codes
POST   /v1/pairing-codes/{code}/approve

POST   /v1/deliveries/claim
POST   /v1/deliveries/{delivery_id}/ack
POST   /v1/deliveries/{delivery_id}/fail
```

后续可信内部发布可单独增加：

```text
POST   /v1/namespaces/{namespace}/events
```

该接口必须使用独立的发布者身份、命名空间授权、schema 校验和幂等键，不能复用普通用户身份。

统一错误响应：

```json
{
  "code": "EVENT_NOT_FOUND",
  "message": "不支持该事件",
  "request_id": "..."
}
```

API 要求：

- 仅绑定容器或内网接口，不暴露公网；Webhook 入口需要独立网关和安全策略。
- 除健康检查外，所有内部接口校验服务身份。
- 管理员写操作再次从数据库检查操作者账号为 `active`、`command=true` 且角色为 `admin`。
- `grant`、`revoke` 请求必须显式指定 `permissions: ["chat"]`、`["command"]` 或两者，重复调用保持幂等。
- 权限查询分别返回账号状态、角色、`chat` 和 `command`，不得只返回笼统 `enabled`。
- 重复订阅或取消保持幂等，不返回 500。
- 管理员不能通过普通 API 移除最后一个启用管理员。
- 待投递领取使用数据库事务和行级并发控制，返回短期租约。
- `ack`、`fail` 只有在 `delivery_id` 与当前有效租约匹配时才能更新状态。
- 所有配置、发布和管理员操作记录审计日志。

### 11.1 双维度权限响应

```json
{
  "platform": "qqbot",
  "openid": "CURRENT_USER_OPENID",
  "account_status": "active",
  "role": "user",
  "permissions": {
    "chat": false,
    "command": true
  }
}
```

语义：

- `chat=true` 仅允许普通消息进入 Hermes LLM，不隐含命令权限。
- `command=true` 允许 Plugin 中策略为 `authorized` 的命令，不隐含聊天权限。
- `role=admin` 必须同时满足 `account_status=active` 和 `command=true` 才可执行管理员命令。
- 角色修改不隐式授予或撤销 `chat`、`command` 权限。
- 不存在的用户返回明确的 `account_status=unknown` 或等价状态，不能与服务错误混淆。
- `account_status=blocked` 优先拒绝所有操作，包括 Plugin 配置为 `public` 的命令。

授权请求示例：

```json
{
  "permissions": ["command"],
  "operator_openid": "OPENID_OF_ADMIN"
}
```

撤销 `command` 权限时保留订阅记录但暂停生效，不再为该用户创建新的 delivery。重新授予后恢复后续新事件投递，但不补发停权期间的历史事件。

## 12. 通用事件处理与发布流程

```text
调度/接收原始数据
  -> provider 校验与规范化 Observation
  -> 保存 checkpoint/必要观察状态
  -> trigger 比较并产生 DomainEvent
  -> 以 event_key + dedupe_key 幂等写入 event_occurrences
  -> 查询该规范 event_key 且账号 active、command=true 的有效订阅者
  -> renderer 生成 DeliveryMessage
  -> 同一事务创建唯一 deliveries
  -> Plugin 领取并通过 Hermes 私聊
  -> Plugin 回写 ack/fail
```

关键规则：

- 首次观察默认只建立基线；是否发布初始事件由 provider 元数据显式声明。
- 数据源失败时保留旧 checkpoint，不产生“消失”或“恢复”事件。
- 事件发生记录和 delivery 创建必须可恢复、可审计。
- provider 重试与 delivery 重试相互独立。
- renderer 失败不得破坏已经发布的 DomainEvent，应进入可观察的失败状态。
- 订阅只允许指向已注册、可订阅且启用的事件类型。
- `chat` 权限不影响事件订阅和通知；事件命令及通知资格只使用 `command` 权限。

## 13. Plugin 拉取式投递契约

Plugin 周期性调用 `POST /v1/deliveries/claim`。EventServer：

- 只领取 `pending` 或到达 `next_attempt_at` 的 `retry` 记录。
- 将过期 `leased` 记录恢复为可领取，但不影响有效租约。
- 在同一事务中锁定候选记录并更新为 `leased`。
- 返回 `delivery_id`、一次性租约 Token、目标平台/OpenID、通知内容、当前尝试次数和通用 `event_id/event_key`。
- 不向 Plugin 暴露 provider 私有配置、数据库内部字段或原始数据源响应。

Plugin 发送成功后调用 `ack`；失败后调用 `fail` 并报告脱敏错误码及是否可重试。EventServer 校验租约后更新为 `sent`、`retry` 或 `dead`。达到最大次数、明确不可重试或消息非法时进入 `dead`，不得无限重发。

该方案是至少一次投递。若 Plugin 发送成功后在 `ack` 前崩溃，租约超时后可能重复发送；若 Hermes/QQ 支持幂等键，统一使用稳定 `delivery_id`，否则必须在运维文档中说明限制。

## 14. 新事件接入流程

新增事件不得修改鉴权、订阅或 outbox 核心逻辑。标准流程如下：

1. 选择新的命名空间和稳定 `event_key`。
2. 编写事件说明、payload JSON Schema、示例和兼容策略。
3. 确定接入模式：轮询、定时、Webhook 或可信内部发布。
4. 实现独立 provider adapter、normalizer、trigger 和 renderer。
5. 为原始数据异常、超时、重复输入、首次基线和状态变化编写 fixture 测试。
6. 运行通用 provider 合规测试，验证禁止访问订阅和发送层。
7. 在测试环境注册 provider，观察采集结果但暂不开放订阅。
8. 验证 `dedupe_key`、事件 schema、消息模板和故障隔离。
9. 启用事件类型并开放订阅。
10. 记录负责人、数据源、轮询频率、速率限制、SLA 和下线流程。

每个 provider 的最低交付物：

- `ProviderMetadata` 与配置 schema。
- Observation 和 DomainEvent schema。
- 正常、无变化、异常、超时和重复数据 fixture。
- trigger 幂等测试和 renderer 快照测试。
- 数据源使用限制、负责人和故障处理说明。
- 指标：调用次数、耗时、失败数、最近成功时间、发布事件数。

## 15. 故障隔离与可观测性

- scheduler 为每个 provider 设置独立超时、并发限制和熔断状态。
- 单个 provider 连续失败不得阻塞其他 provider、权限 API 或 delivery API。
- 日志统一包含 `request_id`、`provider_key`、`event_id` 或 `delivery_id`，OpenID 默认脱敏。
- 指标至少覆盖 provider 健康、调度延迟、事件产生数、待投递积压、租约过期数、重试数和 dead 数。
- `/health` 区分进程存活与依赖就绪；非关键 provider 故障不应让整个服务失去存活状态。
- 对异常积压、连续 provider 失败、Webhook 验签失败激增和 dead delivery 建立告警。

## 16. 计划实施步骤

截至 2026-09-14，步骤 1—10 已在默认离线套件和显式开启的 MySQL 测试套件中完成；
步骤 11 仅完成容器构建文件和基础运维说明，备份恢复与生产部署待后续授权。

1. 建立通用 EventServer 项目和锁定依赖。
2. 定义 Observation、DomainEvent、DeliveryMessage 和 provider 协议。
3. 实现配置校验、结构化日志、Secret 脱敏、指标与健康检查。
4. 建立 MySQL 连接层、通用数据模型、迁移和测试数据库策略。
5. 实现用户状态、`chat`/`command` 双维度权限、角色、订阅、事件目录和 pairing code API。
6. 实现 provider 注册、调度租约、故障隔离和 checkpoint。
7. 实现事件幂等发布、renderer 和 delivery outbox。
8. 实现 delivery 领取、租约、`ack`/`fail`、有限重试和过期恢复。
9. 将 Warframe 三类事件实现为首批 provider，验证核心层无 Warframe 依赖。
10. 完成 provider 合规、API、数据库、并发和端到端测试。
11. 完成容器、备份恢复、扩展开发和部署说明。

## 17. 验证命令

以下命令已在 `autoqq_test` 验证；MySQL 集成套件仍需显式开关：

```bash
# 迁移状态
alembic current
alembic upgrade head

# 默认离线套件
pytest

# 隔离 MySQL 集成套件
RUN_MYSQL_INTEGRATION=1 pytest

# 本地服务（待执行）
uvicorn eventserver.main:app --host 0.0.0.0 --port 8080
```

默认测试不得连接 `192.168.1.4:3309` 或真实外部数据源。所有 provider 使用 fixture 或 mock；目标环境测试必须显式开启。

## 18. 备份、升级与回退

- 迁移前对 `autoqq` 做一致性备份并验证可读。
- 记录应用版本、Alembic revision、事件 schema 和启用 provider 清单。
- 先让 Plugin 停止领取新任务，再停止 scheduler，等待已领取任务完成或租约到期。
- 数据库迁移与应用发布分开观察，优先采用向后兼容迁移。
- provider 或模板升级必须兼容已保存事件；不兼容时提升 schema 版本。
- 回退前确认旧应用能够读取新 schema；不可逆迁移必须单独审批。
- 恢复演练必须验证已发布事件和已完成 delivery 不会被无条件重复生成。

## 19. 验收清单

- [x] 核心包不导入任何 Warframe provider 或领域模型。
- [x] Warframe 三类事件通过与其他事件相同的 provider 接口注册。
- [ ] 新增示例事件无需修改鉴权、订阅、outbox 或 Plugin 代码。
- [x] 支持至少轮询型接入，并为定时、Webhook 和可信内部发布保留清晰接口。
- [x] 事件键有命名空间、schema 版本、别名迁移和下线规则。
- [x] provider 故障相互隔离，不阻塞核心 API 和其他 provider。
- [x] 只有 EventServer 持有 MySQL 配置并直接访问数据库。
- [x] `chat` 和 `command` 权限独立存储、独立授予和独立撤销。
- [x] 管理员身份必须同时满足 active、`command=true` 和 admin 角色。
- [x] 撤销 `command` 后暂停新事件 delivery，重新授予时不补发停权期间事件。
- [x] 数据库口令、内部 Token、Webhook Secret 和真实 OpenID 不进入 Git 或日志。
- [x] 状态不变不发布；首次观察默认只建立基线。
- [x] 事件和 delivery 均由数据库唯一约束保证幂等。
- [x] Plugin 能领取通用待投递消息，EventServer 不直接调用 Hermes 或 QQ API。
- [x] 并发 Plugin 不会同时领取同一有效租约，`ack/fail` 必须匹配租约。
- [x] 单个发送后回写前崩溃造成的重复风险已用幂等键尽量规避并记录。
- [ ] 已验证迁移、备份、恢复、回退和 provider 下线流程。

## 20. 与 Plugin 联调交付物

EventServer 应向 Plugin 提供：

- 内网基址、`/health` 和版本信息。
- OpenAPI 或等价的版本化接口说明。
- 测试环境服务 Token 的安全分发方式。
- 固定错误码、超时和幂等语义。
- 包含账号状态、角色、`chat`、`command` 的稳定权限响应，以及按维度幂等授权/撤销契约。
- 通用 DeliveryMessage、领取租约、`ack/fail` 和重试上限契约。
- 不含敏感数据且不依赖具体领域的契约测试 fixture。

Plugin 只识别通用 `event_key` 和 DeliveryMessage，不得导入 Warframe provider 模型。任何不兼容接口变更必须先更新契约、版本和双方测试。
